{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "RUN",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
              },
            "command": "clang MicroFramework/*.cpp -framework OPENGL -framework GLUT -o mf.app ; ./mf.app"
        }
    ]
}
